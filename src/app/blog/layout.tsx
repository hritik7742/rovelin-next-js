export const metadata = {
  title: 'Blog | Rovelin Studio',
  description: 'Discover insights, strategies, and updates from the Rovelin team about Chrome extensions, web development, and software solutions.',
};

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 